import React from 'react';
import { Flex , Box } from '@chakra-ui/react';

export const Card = ({}) => {
    return (
        <Box bg='red.100' bgGradient='linear(to-r, red.100, blue.200)' w={520} h={720} >        
        </Box>
    );
}